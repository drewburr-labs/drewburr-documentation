apiVersion: v1
kind: Service
metadata:
  name: {{ include "printstream.fullname" . }}
  labels:
    {{- include "printstream.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "printstream.selectorLabels" . | nindent 4 }}
