apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "printstream.fullname" . }}-test-connection"
  labels:
    {{- include "printstream.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "printstream.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
